# PMP dedicated ports (MUST NOT overlap with any other project)
PMP_DB_HOST_PORT=55432
PMP_API_HOST_PORT=58080

# Example creds (change in your local .env.pmp, do NOT commit secrets)
PMP_DB_USER=pmp
PMP_DB_PASSWORD=change_me
PMP_DB_NAME=pmp
